% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spacelink_ctSVG.R
\name{spacelink_ctSVG}
\alias{spacelink_ctSVG}
\title{Cell Type-Specific Spatial Variability Analysis}
\usage{
spacelink_ctSVG(
  normalized_counts,
  spatial_coords,
  cell_type_proportions,
  focal_cell_type,
  covariates = NULL,
  lite = FALSE,
  grid_size = NULL,
  kernel = "Exponential",
  n_lengthscales = 5,
  c1 = 0.1,
  c2 = 0.1,
  n_workers = 1
)
}
\arguments{
\item{normalized_counts}{Log-transformed normalized count matrix (genes x spots). Accepts a
numeric matrix, data.frame, or sparse matrix (\code{sparseMatrix}).}

\item{spatial_coords}{Two-dimensional spatial coordinate matrix (spots x 2).
Accepts a numeric matrix or data.frame.}

\item{cell_type_proportions}{Cell type proportion matrix (spots x cell types,
e.g., estimated by RCTD). Accepts a numeric matrix or data.frame.}

\item{focal_cell_type}{Name of the cell type to be tested. Must match a
column name of \code{cell_type_proportions}.}

\item{covariates}{Optional covariate matrix (spots x covariates, without
intercept). Default is \code{NULL}.}

\item{lite}{Logical. If \code{TRUE}, uses a grid-based approximation.
Default is \code{FALSE}.}

\item{grid_size}{Grid cell size for lite mode. If \code{NULL}, set
automatically to 5 times the minimum nearest-neighbor distance.
Default is \code{NULL}.}

\item{kernel}{Spatial kernel type. One of \code{"Exponential"} (default) or
\code{"Gaussian"}.}

\item{n_lengthscales}{Number of length scales (kernels). Default is 5.}

\item{c1}{Gating parameter controlling the proportion threshold for
determining if the focal cell type dominates. Default is 0.1.}

\item{c2}{Gating parameter controlling the colocalization threshold.
Default is 0.1.}

\item{n_workers}{Number of workers for parallelization. Default is 1.}
}
\value{
A data frame (genes x results) containing:
\describe{
\item{time}{Computation time per gene (seconds)}
\item{pval}{Combined p-value (ACAT)}
\item{padj}{Benjamini-Hochberg adjusted p-value}
\item{ESV}{Cell type-specific Effective Spatial Variability score}
\item{ESV_adj}{Cell type-specific ESV adjusted for non-ctSVGs (0 if padj > 0.05)}
}
If all spots are pure (no mixing), falls back to \code{\link{spacelink}}
output (same columns) for the focal cell type spots.
}
\description{
Conducts hypothesis testing to identify cell type–specific spatially variable genes (ctSVGs)
using a data-driven colocalization gating strategy,
and provides cell type–specific Effective Spatial Variability (ct-ESV) scores (ranging from 0 to 1),
where higher values indicate greater spatial variability in gene expression within the focal cell type.
}
\examples{
\donttest{
library(spacelink)
set.seed(123)
n_spots    <- 100
n_genes    <- 10
n_celltypes <- 2

coords <- expand.grid(
  x = seq(0, 10, length.out = sqrt(n_spots)),
  y = seq(0, 10, length.out = sqrt(n_spots))
)

cell_type_props <- matrix(nrow = n_spots, ncol = n_celltypes)
cell_type_props[1:(n_spots / 2), 1]            <- runif(n_spots / 2, 0, 0.5)
cell_type_props[(n_spots / 2 + 1):n_spots, 1]  <- runif(n_spots / 2, 0.5, 1)
cell_type_props[, 2] <- 1 - cell_type_props[, 1]
colnames(cell_type_props) <- paste0("Cell_type_", 1:n_celltypes)

expr_data <- matrix(nrow = n_genes, ncol = n_spots)
rownames(expr_data) <- paste0("Gene_", 1:n_genes)
D <- as.matrix(dist(coords)); K <- exp(-D)
Sigma <- chol(K) \%*\% diag(cell_type_props[, "Cell_type_1"])
for (i in 1:(n_genes / 2))
  expr_data[i, ] <- rnorm(n_spots) + i * rnorm(n_spots) \%*\% Sigma
Sigma <- chol(K) \%*\% diag(cell_type_props[, "Cell_type_2"])
for (i in (n_genes / 2 + 1):n_genes)
  expr_data[i, ] <- rnorm(n_spots) + rnorm(n_spots) \%*\% Sigma

results <- spacelink_ctSVG(
  normalized_counts     = expr_data,
  spatial_coords        = coords,
  cell_type_proportions = cell_type_props,
  focal_cell_type       = "Cell_type_1"
)
print(results[, c("pval", "ESV")])
}

}
